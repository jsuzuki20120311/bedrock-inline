export type CompletionSettings = {
  profile: string;
  region: string;
  accessKeyId: string;
  secretAccessKey: string;
  modelId: string;
  temperature: number;
  topP: number;
  maxTokens: number;
};
